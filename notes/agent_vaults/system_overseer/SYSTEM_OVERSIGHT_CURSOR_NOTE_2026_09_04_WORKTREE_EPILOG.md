- **2026-09-04 stagnation dispatch (worktree-epilog self-check)**
  - **Found:** high `peer_orchestrate self-check failed: tests not ok: tests: FAIL — (pass --execute…)` — false red. `project_automation._inconclusive_quick_fail_line` already soft-passes the peer_worktree dry-run epilog, but `_tests_detail_inconclusive` only soft-passed bare `failed` / fail-ttl — cached/raced FAIL detail with epilog still became ISSUES. Mac↔DGX rsync thrashed `scripts/peer_orchestrate.py` (~15s) between bare-fail tip and token-list tip (neither had `pass --execute`).
  - **Fixed:** permanent `_tests_detail_inconclusive` merge — soft toks (lock/deferred/skipped/storm/`pass --execute`/`never uses --force`) + bare-fail + hard `failures=`/`errors=`; needles `OVERSEER_INCONCLUSIVE_WORKTREE_EPILOG_SELF_CHECK_2026_09_04` + shared `OVERSEER_INCONCLUSIVE_WORKTREE_EPILOG_2026_09_04`; restore `live_bad`/`vault_ok` require epilog; hub-protect vault pin; unittest extended; 180s tip-beat vs Mac clobber. Self-check ISSUES: none; test-quick green; factory **96%→99%** (+3).
  - **Still broken:** Mac rsync still rewinds unprotected WORKING scripts within seconds unless restore/beat wins; Dispatch capped ~95% on dirty tree; Creative registry verify items deferred under `self_sufficient`.
  - **Needs human:** Exclude `scripts/peer_orchestrate.py` (hub-protect set) from Mac sender rsync `--delete-before`; commit WORKING scripts when safe.

