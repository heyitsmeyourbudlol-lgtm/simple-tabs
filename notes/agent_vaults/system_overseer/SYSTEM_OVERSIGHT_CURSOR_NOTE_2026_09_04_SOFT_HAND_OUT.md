# Cursor overseer note — 2026-09-04 soft-exit + hand_out

_Stagnation dispatch. Durable copy — SYSTEM_OVERSIGHT.md is mechanically rewritten._

## Found
- Dispatch: cursor-agent non-zero (Backend Engineer ← Fix verify gate) nested instant-react via hand_out/role-arrow echo
- Critical tests FAIL intermittent — Mac rsync mid-unittest + soft-exit land thrash
- `peer_loop` repeatedly rewinded to bare `verify_ok=False` / `note="cursor-agent non-zero exit"`
- Hub-protect vault sometimes held pre-soft poison (restore amplified clobber)

## Fixed
- Soft Episodic: `verify_ok=bool(tests_still_ok)` + soft notes without instant-react substring
- `OVERSEER_HAND_OUT_AGENT_EXIT_ECHO_2026_09_04` in `peer_oversight_events._skip_live_hit_line`
- Beat needles + multi-pin (vault / overseer-land / notes vault / golden)
- Active open 0; factory 94–95% (+4–5 from dispatch 90%); soft suite green under land-hold

## Still broken / needs human
- Dirty Dispatch ~85%; Mac→DGX rsync rewinds unprotected scripts within seconds
- Exclude hub-protect + overseer-land from Mac sender rsync; commit WORKING scripts when safe
